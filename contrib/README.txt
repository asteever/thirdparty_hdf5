The purpose of this subdirectory of the HDF5 repository is to hold new tags
for releases and significant events within the trunk.

Each 'significant event' tag should have a "TAG.txt" file in its top
subdirectory, describing the purpose of the branch, and its current status.
